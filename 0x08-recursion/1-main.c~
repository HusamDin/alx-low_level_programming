#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *s = "Hello World";
    int i = strlen(s) - 1;
    printf("%d\n", i);
    return (0);
}
