/**
 * string_toupper - Changes all lowercase letters to uppercase
 * @s: The uppercased string
 *
 * Return: s
 */

char *string_toupper(char *s)
{
	while (*s != '\0')
	{
		if (*s >= 97 && *s <= 122)
			*s -= 32;
	       	s++;
	}
	return (s);
}
